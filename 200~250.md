## 227 
#### 조건
- 문자열과 한줄에 입력 할 글자 수를 입력 받는다.
- 구현할 함수의 이름은 print_mxn으로 지정한다.
<br>
<br>
#### 선언부 입력
```
def print_mxn(a,b):
```
- def를 통해 함수의 이름을 지정하고 선언할 수 있었다.
- 입력 받아야 할 인자의 수가 문자열, 글자 수 두개 이기 때문에 a, b 두 개의 인자를 설정해 주었다.
<br>
<br>
#### 구현부 입력
```
    ind = int(len(a)/b)
    for i in range(int(len(a)/b) + 1) :
        print(a[i*ind:(i*ind)+ind])

print_mxn("아이엠어보이유알어걸", 3)
```
- ind는 **입력받은 문자열(a)** 을 **입력 받은 글자 수(b)** 로 분배 해 놓아야 하기 때문에 **len(a)/b** 를 통해 어떤 문자열과 글자 수 가 들어오더라도 대처 할 수 있도록 설정했다.
- for문을 설정할 때 range 부분을 **len(a)/b** 에 맞추게 될 경우 int로 인해 잘리게 된 나머지 부분이 있을 수 있기 때문에 +1 을 추가해 줌으로써 절삭된 부분까지 출력될 수 있도록 했다.
- 개선사항으로는 해당 문제가 예시로 **print_mxn("아이엠보이유알어걸", 3)** 이라고 지칭되어 있었기 때문에 상관 없었지만, 조금 더 범용적으로 사용하기 위해서는 a와 b 모두 Input을 할당해 주는것이 좋겠다는 생각을 했다.
<br>
<br>

#### 보완내용
```
def print_mxn(a,b):
    ind = int(len(a)/b)
    print(ind, len(a), b)
    for i in range(int(len(a)/b) + 1) :
        print(a[i*ind:(i*ind)+ind])

c = input("문자열을 입력해 주세요: ")
d = int(input("문자열을 분할하고 싶은 글자의 수를 설정해 주세요 : "))
print_mxn(c, d)
```
- 보완한 코드는 다음과 같으며 코드를 실행 했을 때의 결과를 통해 입력한 b와 ind의 값이 같지 않으면 현재 코드에서는 사용자가 원하는 결과를 도출할 수 없다는 결론을 내게 되었다.
- 이는 인덱스 부분의 문제라고 판단하고 다시 코드를 분석해 본 결과, for의 print문에서 슬라이싱이 **i * ind:(i * ind)+ind** 에서 **i * b:(i * b)+b**로 변경되어야 한다는 것을 발견하였다.
- 따라서, 보완된 구현부는 다음과 같다.

#### 최종 코드

```
def print_mxn(a,b):
    ind = int(len(a)/b)
    print(ind, len(a), b)
    for i in range(ind + 1) :
        print(a[i*b:(i*b)+b])

c = input("문자열을 입력해 주세요: ")
d = int(input("문자열을 분할하고 싶은 글자의 수를 설정해 주세요 : "))
print_mxn(c, d)

```

